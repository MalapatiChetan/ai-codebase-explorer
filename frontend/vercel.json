{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "env": {
    "NEXT_PUBLIC_API_URL": "@api_url"
  },
  "regions": ["sfo1"],
  "functions": {
    "pages/api/**": {
      "maxDuration": 60
    }
  }
}
